const sumar = (a,b) => a + b
const restar = (a,b) => a - b

export const aritmetica = { 
    sumar, // si el nombre y el valor tienen el mismo nombre en un objeto literal,
    restar // se puede poner sólo el valor, en vez de restar : restar 
}

